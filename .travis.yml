language: cpp
sudo: false

addons:
  apt:
    packages:
    - build-essential
    - gnome-common
    - libglib2.0-dev
    - libopenexr-dev
    - gthumb-dev
    - libgconf2-dev
    - libgtk-3-dev
    - gthumb-dev
    - gnome-doc-utils
  dist: bionic
  os: linux

script: ./autogen.sh --prefix=/usr && ./configure && make
